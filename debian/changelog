gnome-customizer (0.3.25) resolute; urgency=high

  * Make factory reset explicitly select the packaged Yaru or GNOME GDM resource.
  * Clear local GDM overrides, user settings, and monitor state during factory reset.
  * Reset every exposed desktop, Shell, and dock setting even when changed elsewhere.
  * Keep pre-Customizer state restoration separate from factory reset.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Thu, 13 Aug 2026 16:10:00 -0500

gnome-customizer (0.3.24) resolute; urgency=high

  * Verify that login reset switches back to a stock GDM resource.
  * Remove all managed login overrides, assets, state, and staged changes.
  * Restart an already-running system helper during package upgrades.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Thu, 13 Aug 2026 15:15:00 -0500

gnome-customizer (0.3.23) resolute; urgency=high

  * Save applied login-screen appearance when exporting the current theme.
  * Embed desktop and login wallpaper images, plus the login logo, in themes.
  * Recover previously applied managed GDM appearance during theme export.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Thu, 13 Aug 2026 15:20:00 -0500

gnome-customizer (0.3.22) resolute; urgency=high

  * Add optional alphabetical ordering for Overview's application grid and folders.
  * Add Ubuntu Dock's native Shrink the Dash control and theme round-tripping.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Thu, 13 Aug 2026 14:41:00 -0500

gnome-customizer (0.3.21) resolute; urgency=high

  * Add native placement controls for centered new windows and desktop-icon corners.
  * Remove the inherited Yaru menu shadow that mismatched custom gradient radii.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Thu, 13 Aug 2026 04:50:00 -0500

gnome-customizer (0.3.20) resolute; urgency=high

  * Normalize GNOME wallpaper colors when saving the current appearance as a theme.
  * Prevent an unsupported optional wallpaper color from blocking theme export.
  * Remove the unreliable Theme Builder while retaining saved-theme workflows.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 23:59:00 -0500

gnome-customizer (0.3.19) resolute; urgency=high

  * Replace the misleading Stage Theme action with an immediate Apply Theme action.
  * Add confirmed deletion for imported and saved local themes.
  * Keep included sample themes protected from deletion.
  * Test restoration after deliberately changing accent, Shell, and native Dock values.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 23:45:00 -0500

gnome-customizer (0.3.18) resolute; urgency=high

  * Save and restore every supported desktop appearance and layout setting.
  * Preserve all native Ubuntu Dock/Dash-to-Dock values, including position and Panel Mode.
  * Preserve disabled Top Bar and Blur surface values for exact later restoration.
  * Verify the complete theme snapshot through archive, staging, Apply, and GSettings reads.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 23:00:00 -0500

gnome-customizer (0.3.17) resolute; urgency=high

  * Save the currently applied appearance as a reusable .gctheme archive.
  * Round-trip desktop themes, wallpapers, Shell surface states, and dock appearance.
  * Apply opacity to overview hover backgrounds instead of fading application icons.
  * Add an independent tint color for overview and app-grid hover backgrounds.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 21:30:00 -0500

gnome-customizer (0.3.16) resolute; urgency=high

  * Match Ubuntu GNOME 50's native accent-to-Yaru theme mapping exactly.
  * Keep GTK, folder icons, and light/dark variants synchronized on Apply.
  * Repair neutral Yaru themes left behind by the 0.3.15 regression.
  * Verify accent folder assets and both appearance modes in real GNOME Shell.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 19:00:00 -0500

gnome-customizer (0.3.15) resolute; urgency=high

  * Keep GNOME 50's native accent-color setting authoritative.
  * Migrate stale colored Yaru GTK and icon variants to neutral Yaru themes.
  * Add configurable opacity for hovered overview and app-grid icons.
  * Exercise the real Appearance accent control and Apply path in GNOME Shell.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 18:00:00 -0500

gnome-customizer (0.3.14) resolute; urgency=high

  * Apply top-bar color, opacity, radius, and blur directly to GNOME's panel.
  * Wait for Shell startup and reapply popup blur when menus are mapped.
  * Enable the companion automatically when a Shell surface setting is applied.
  * Add an isolated real-GNOME-Shell lifecycle and visual-effect smoke test.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 17:30:00 -0500

gnome-customizer (0.3.13) resolute; urgency=high

  * Remove the replacement dock and stop suppressing Ubuntu Dock or Dash to Dock.
  * Configure the installed GNOME dock extension directly through its native settings.
  * Apply supported dock theme values without enabling the Shell companion.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Wed, 12 Aug 2026 15:00:00 -0500

gnome-customizer (0.3.12) resolute; urgency=high

  * Keep the Ubuntu Show Applications icon color fixed across light and dark modes.
  * Preserve the native Ubuntu icon shape and original gray fill.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 22:00:00 -0500

gnome-customizer (0.3.11) resolute; urgency=high

  * Let maximized windows use the full screen in auto-hide modes.
  * Reveal auto-hidden and intelligently hidden docks over windows without resizing them.
  * Reserve dock-height work area only when both hide modes are disabled.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 21:45:00 -0500

gnome-customizer (0.3.10) resolute; urgency=high

  * Stop auto-hide and intelligent-hide hover oscillation.
  * Reveal hidden docks from a stable three-pixel screen-edge trigger.
  * Keep docks visible while the pointer is over either the dock or its edge trigger.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 21:35:00 -0500

gnome-customizer (0.3.9) resolute; urgency=high

  * Preserve custom top-bar transparency when Show Applications closes.
  * Restore dynamic top-bar styling after GNOME Shell clears the panel style.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 21:20:00 -0500

gnome-customizer (0.3.8) resolute; urgency=high

  * Keep the custom panel background inside the real panel so the top bar stays at the screen edge.
  * Move floating docks closer to the bottom edge without shifting icons upward.
  * Place running-app indicators directly beneath their icons.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 21:00:00 -0500

gnome-customizer (0.3.7) resolute; urgency=high

  * Preserve custom top-bar opacity in Overview and Show Applications.
  * Reserve a fixed indicator lane so running apps never move dock icons.
  * Add a None selection for running indicators and theme packages.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 20:40:00 -0500

gnome-customizer (0.3.6) resolute; urgency=high

  * Render the real desktop wallpaper behind the overview and app grid.
  * Keep the wallpaper actor correctly positioned in GNOME Shell 50.
  * Scale overview blur strength correctly on HiDPI displays.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 20:10:00 -0500

gnome-customizer (0.3.5) resolute; urgency=high

  * Use Ubuntu's native Show Applications icon unchanged in every color mode.
  * Remove incorrect forced light/dark icon recoloring.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 19:15:00 -0500

gnome-customizer (0.3.4) resolute; urgency=high

  * Recolor the Show Applications icon immediately with GNOME light/dark mode.
  * Remove dark Yaru GTK and icon names left pinned by legacy releases in light mode.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 19:05:00 -0500

gnome-customizer (0.3.3) resolute; urgency=high

  * Render visible high-contrast running indicators in the custom dock.
  * Give dot, dash, and line indicators reliable minimum dimensions.
  * Apply the selected dock corner radius whether or not Floating is enabled.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 18:50:00 -0500

gnome-customizer (0.3.2) resolute; urgency=high

  * Render zero dock opacity as explicitly transparent.
  * Disable the dock background blur at zero opacity.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 18:35:00 -0500

gnome-customizer (0.3.1) resolute; urgency=medium

  * Allow custom dock opacity to reach fully transparent zero.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 18:20:00 -0500

gnome-customizer (0.3.0) resolute; urgency=high

  * Make GNOME Settings authoritative for native light/dark application appearance.
  * Remove the application-theme CSS writer from all normal apply paths.
  * Automatically remove legacy managed GTK CSS before the application starts.
  * Require explicit opt-in before styling the panel, menus, dock, or overview.
  * Restore native Shell surface styles whenever their customization is disabled.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 18:00:00 -0500

gnome-customizer (0.2.7) resolute; urgency=high

  * Never paint the transparent DING desktop window with application colors.
  * Preserve the real desktop wallpaper while Windows & Files themes are active.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 17:12:00 -0500

gnome-customizer (0.2.6) resolute; urgency=high

  * Make Prefer Light and Prefer Dark switch matching GTK and icon themes.
  * Switch an existing managed application palette with the appearance mode.
  * Use real Ubuntu wallpaper URIs when profile schema defaults are empty.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 17:05:00 -0500

gnome-customizer (0.2.5) resolute; urgency=high

  * Automatically enable the Shell companion when the custom dock is enabled.
  * Remove the companion from GNOME's disabled-extension list transactionally.
  * Re-enable GNOME's master extension switch as part of dock activation.
  * Ignore late dock callbacks safely while GNOME Shell is shutting down.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 16:50:00 -0500

gnome-customizer (0.2.4) resolute; urgency=high

  * Remove unsafe global Libadwaita semantic-color overrides.
  * Exclude GNOME Customizer from managed application-theme selectors.
  * Migrate existing managed GTK CSS before the application starts.
  * Restore Ubuntu Yaru defaults even in sessions missing the Ubuntu profile.
  * Synchronize Ubuntu and GNOME color-scheme keys in profile-less sessions.
  * Reject unsafe text/background contrast before writing application CSS.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 16:30:00 -0500

gnome-customizer (0.2.3) resolute; urgency=high

  * Re-anchor the custom dock after GNOME Shell completes startup allocation.
  * Keep every dock position clamped inside its assigned monitor.
  * Reset every managed desktop, Shell, application, wallpaper, and GDM change.
  * Clarify Windows & Files and add visible Light, Dark, and High Contrast presets.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 16:10:00 -0500

gnome-customizer (0.2.2) resolute; urgency=high

  * Match the session's native Show Applications icon.
  * Toggle the app grid closed on a second click.
  * Allow the Show Applications button at the first or last dock position.
  * Render a wallpaper-backed overview blur with tint, brightness, and saturation.
  * Display Night Light schedules in the user's 12- or 24-hour clock format.
  * Apply opacity to surface colors and gradients without fading their contents.
  * Replace hexadecimal color text fields with native GNOME color dialogs.
  * Complete mouse and touchpad controls and verify every applied value.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 15:50:00 -0500

gnome-customizer (0.2.1) resolute; urgency=high

  * Keep a native Show Applications button in the custom dock.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 05:20:00 -0500

gnome-customizer (0.2.0) resolute; urgency=medium

  * Add complete controlled GTK3, GTK4, Libadwaita, and Files palettes.
  * Preserve and restore unrelated user CSS transactionally.
  * Suppress an existing Ubuntu Dock actor while the custom dock is active.
  * Expand Theme Builder and sample themes with Files/application previews.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 05:05:00 -0500

gnome-customizer (0.1.2) resolute; urgency=high

  * Permit administrator authentication for remote and sessionless app launches.
  * Stage wallpapers in immutable content-addressed files.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 04:50:00 -0500

gnome-customizer (0.1.1) resolute; urgency=high

  * Create managed helper paths before systemd sandbox activation.
  * Apply the primary wallpaper to both light and dark desktop modes.
  * Show privileged apply failures in a persistent dialog.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 04:40:00 -0500

gnome-customizer (0.1.0) resolute; urgency=medium

  * Initial release for GNOME 50.1 and Ubuntu 26.04 LTS.

 -- GNOME Customizer Contributors <gnome-customizer@example.invalid>  Tue, 11 Aug 2026 03:00:00 -0500
